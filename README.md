# Dashboard + Blog

Single site combining an Admin Dashboard and a Blog app.

- Dashboard: responsive admin UI with stats, user management, create user dialog
- Blog: posts listing, detail, create/delete via JSONPlaceholder + React Query

Run:

npm install
npm run dev

